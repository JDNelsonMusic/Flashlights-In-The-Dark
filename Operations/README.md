# Operations

`Scripts/` owns deterministic generation and validation; `Tools/` holds concert simulation and support utilities; `Light-Chorus/` builds MIDI spreadsheet material; and `Fastlane/` contains iOS release support.

Run tools from the repository root unless their own documentation states otherwise.
