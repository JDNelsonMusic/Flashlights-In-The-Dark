# DAW Production

`ProTools-Session/` is a local ignored workspace. `Exports/` contains approved tracked reference renders, and `Audits/` contains generated timing and asset reports.

Close Pro Tools before relocating the session. Verify the stated source identity and timing before promoting a new export into the show-control workflow.
