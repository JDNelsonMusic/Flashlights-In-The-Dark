# Temporary demo-work boundary

This directory contains only `demo/`, which has unresolved local video work. It remains at its legacy path to preserve that work exactly.

Do not add new documentation here. Put new guides in `Documentation/` and new committed visual output in `Visual-Production/`. Move `demo/` only after its owner has resolved and committed the current changes.
